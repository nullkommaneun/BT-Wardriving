export class MapView{constructor(){this.map=null;this.markers=L.layerGroup();this.heat=null;this.hasHeat=!!L.heatLayer}init(domId='map'){this.map=L.map(domId,{zoomControl:true});L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap'}).addTo(this.map);this.markers.addTo(this.map);this.map.setView([51.1657,10.4515],5)}clear(){this.markers.clearLayers();if(this.heat){this.map.removeLayer(this.heat);this.heat=null}}addRecord(rec){if(typeof rec.latitude!=='number'||typeof rec.longitude!=='number')return;const popup=[`<b>${rec.deviceName||'(kein Name)'}</b>`,`Zeit: ${rec.timestamp}`,`RSSI: ${rec.rssi} dBm`,`UUIDs: ${rec.serviceUUIDs&&rec.serviceUUIDs.length?rec.serviceUUIDs.join(', '):'(leer)'}`,`Pos: ${rec.latitude.toFixed(6)}, ${rec.longitude.toFixed(6)}`].join('<br>');const m=L.marker([rec.latitude,rec.longitude]);m.bindPopup(popup);this.markers.addLayer(m)}fitToData(){const b=this.markers.getLayers().map(m=>m.getLatLng());if(b.length){this.map.fitBounds(L.latLngBounds(b),{padding:[24,24]})}}setHeat(rs){if(!this.hasHeat)return;if(this.heat){this.map.removeLayer(this.heat);this.heat=null}if(!rs.length)return;const toW=r=>{const mi=-90,ma=-30;const c=Math.max(mi,Math.min(ma,r));return(c-mi)/(ma-mi)};const pts=rs.filter(r=>typeof r.latitude==='number'&&typeof r.longitude==='number'&&Number.isFinite(r.rssi)).map(r=>[r.latitude,r.longitude,toW(r.rssi)]);if(!pts.length)return;this.heat=L.heatLayer(pts,{radius:25,blur:15,maxZoom:17});this.heat.addTo(this.map)}}